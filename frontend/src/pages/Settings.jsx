import React from 'react'
export default function Settings(){const [api,setApi]=React.useState(localStorage.getItem('apiBase')||'');const save=()=>{localStorage.setItem('apiBase',api);alert('Saved. Refresh the page.')};return(<div className='container'><h2>Settings</h2><div className='card'><label>API Base URL</label><input placeholder='https://<your-funcapp>.azurewebsites.net/api' value={api} onChange={e=>setApi(e.target.value)}/><button className='btn' onClick={save}>Save</button><div className='small'>Leave blank for local dev: http://localhost:7071/api</div></div></div>)}
